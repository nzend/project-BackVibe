// const { Filter } = require('../../models');

// const getByFilters = async (req, res) => {
//   const { type } = req.params;
//   const result = await Filter.find({ filter: type });

//   res.json(result);
// };

// module.exports = getByFilters;
